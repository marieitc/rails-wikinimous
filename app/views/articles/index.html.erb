<body>
  <nav class="navbar navbar-default text-secondary" role="navigation">
    <div class="container-fluid">
      <a class="navbar-brand" href="/articles">Wikinimous</a>
      <a class="navbar-brand" href="/new">New article</a>
    </div>
  </nav>

  <div class="container">
    <div class="">
        <% @articles.each do |article| %>
            <%= link_to article_path(article) do %>
              <h2><%= article.title %></h2>
            <% end %>
              <p><%= article.content %></p>
        <% end %>
      </div>
  </div>
</body>
